[build]
  # 1. Install libraries (pandas)
  # 2. Run your script to turn Excel into JSON
  command = "pip install -r requirements.txt && python data/convert.py"
  
  # The folder that actually contains the index.html (which is your root folder)
  publish = "."